## Git
- don't add co-authors to commits

## Shortcuts (slash commands)

Custom slash commands live in `.claude/commands/`:

- `/commit` → `.claude/commands/commit.md` — create a Conventional Commit for one logical change
